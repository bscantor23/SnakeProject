package com.schemacatalogos.services;

import java.util.List;
import java.util.stream.Collectors;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.annotation.Primary;
import org.springframework.stereotype.Service;

import com.schemacatalogos.clients.ProductoClienteRest;
import com.schemacatalogos.clients.CategoriaClienteRest;

import com.schemacatalogos.models.Catalogo;

@Service
@Primary
public class ServicesCatalogoFeign implements IServicesCatalogo {

	@Autowired
	private ProductoClienteRest clienteFeign;
	
//	@Override
//	public List<Items> findAll() {
//		return clienteFeign.listar().stream().map(p -> new Items(p,1)).collect(Collectors.toList());
//	}
//	
//	@Override
//	public Items findById(Long id, Integer cantidad) {
//		return new Items(clienteFeign.ver(id), cantidad);
//	}
	
}
